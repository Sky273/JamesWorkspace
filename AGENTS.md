# ResumeConverter Memory

This repository uses an external markdown memory vault:

- `C:\Users\mail\Documents\ResumeConverterVault\ResumeConverter`

When working on ResumeConverter:

1. Treat this vault as the project's external memory.
2. Before substantial analysis, debugging, or design work, read:
   - `index.md`
   - `overview.md`
   - relevant topic or entity pages
3. When durable facts or decisions emerge, update the vault:
   - update or create the relevant page
   - update `index.md`
   - append to `log.md`
4. Follow the vault schema defined in:
   - `C:\Users\mail\Documents\ResumeConverterVault\ResumeConverter\AGENTS.md`
5. Keep raw source material under `raw/` and synthesized knowledge in the wiki pages.

If the vault is unavailable, continue normally and say so explicitly.
