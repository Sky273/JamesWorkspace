# Frontend Environment Configuration
# Copy this file to .env and adjust values as needed

# Enable HTTPS for the frontend dev server
VITE_HTTPS_ENABLED=false

# Port for the frontend dev server
VITE_DEV_SERVER_PORT=5173

# HTTPS port for the backend proxy server
VITE_HTTPS_PORT=3443

# Enable public home page for unauthenticated users
# When true, unauthenticated users see a public landing page at /welcome
# When false (default), unauthenticated users are redirected to /signin
VITE_PUBLIC_HOME=false
